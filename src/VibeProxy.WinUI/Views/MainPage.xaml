<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="VibeProxy.WinUI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sections="using:VibeProxy.WinUI.Views.Sections"
    mc:Ignorable="d"
    x:Name="Root">

    <Grid>
        <NavigationView x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsPaneOpen="False"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        SelectionChanged="OnNavSelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard" Tag="Dashboard">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Home" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Services" Tag="Services">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Library" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Settings" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="About" Tag="About">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Help" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <ScrollViewer x:Name="ContentScrollViewer" 
                              Padding="24"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <Grid MaxWidth="1300" HorizontalAlignment="Left">
                        <sections:DashboardSectionView
                            x:Name="DashboardSection"
                            ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

                        <sections:ServicesSectionView
                            x:Name="ServicesSection"
                            ViewModel="{x:Bind ViewModel, Mode=OneWay}"
                            Visibility="Collapsed" />

                        <sections:SettingsSectionView
                            x:Name="SettingsSection"
                            ViewModel="{x:Bind ViewModel, Mode=OneWay}"
                            Visibility="Collapsed" />

                        <sections:AboutSectionView
                            x:Name="AboutSection"
                            Visibility="Collapsed" />
                    </Grid>
                </ScrollViewer>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Page>
