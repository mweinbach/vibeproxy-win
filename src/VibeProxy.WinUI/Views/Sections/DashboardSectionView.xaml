<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VibeProxy.WinUI.Views.Sections.DashboardSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="24">
        <Border Style="{StaticResource CardBorderStyle}">
            <StackPanel Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE73E;" Foreground="{x:Bind ViewModel.IsServerRunning, Mode=OneWay, Converter={StaticResource StatusBrush}}" />
                    <TextBlock Text="Server Status" VerticalAlignment="Center" FontWeight="SemiBold" />
                    <TextBlock Text="{x:Bind ViewModel.ServerStatusText, Mode=OneWay}" VerticalAlignment="Center" Foreground="Gray" />
                </StackPanel>

                <InfoBar IsOpen="True"
                         Severity="{x:Bind ViewModel.IsServerRunning, Mode=OneWay, Converter={StaticResource StatusToSeverityConverter}}"
                         Title="{x:Bind ViewModel.ServerStatusText, Mode=OneWay}"
                         Message="Proxy server is active and listening for local requests."
                         IsClosable="False" />

                <Button Content="Toggle Server"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="{x:Bind Host.OnToggleServerClicked}"
                        HorizontalAlignment="Left" />
            </StackPanel>
        </Border>

        <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Environment" FontWeight="SemiBold" />
                    <TextBlock Text="Port: 8317 (Thinking), 8318 (Backend)" FontSize="12" Foreground="Gray" />
                    <Button Content="Copy URL" Click="{x:Bind Host.OnCopyUrlClicked}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Auth Files" FontWeight="SemiBold" />
                    <TextBlock Text="Manage your tokens locally" FontSize="12" Foreground="Gray" />
                    <Button Content="Open Folder" Click="{x:Bind Host.OnOpenAuthFolderClicked}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
